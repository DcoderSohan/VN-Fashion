{
  "name": "vn-fashion",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd backend && npm install",
    "build": "npm --prefix frontend install --include=dev && npm --prefix frontend run build && npm --prefix admin install --include=dev && npm --prefix admin run build",
    "start": "cd backend && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
